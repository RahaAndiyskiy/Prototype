Add your audio assets here:

- rain-loop.mp3 — continuous rain ambient loop
- thunder-hit.mp3 — short thunder strike sound

Make sure the files are placed directly in the `public/` folder so they are available at `/rain-loop.mp3` and `/thunder-hit.mp3`.
